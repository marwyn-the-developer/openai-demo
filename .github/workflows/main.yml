name: build and deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'
    steps:
      - name: Checkout
        uses: actions/checkout@v2.5.0
    
      - name: Docker Login
        uses: docker/login-action@v2.1.0
        with:
          registry: https://index.docker.io/v1/
          username: ${{ secrets.dockerhub_username }}
          password: ${{ secrets.dockerhub_password }}
          logout: false
      
      - name: Build and push docker image
        uses: docker/build-push-action@v3.2.0
        with:
          push: true
          tags: index.docker.io/marwynthedev/openai-demo:${{ github.sha }}
          file: ./Dockerfile

      - name: Azure WebApp
        uses: Azure/webapps-deploy@v2.2.5
        with:
          app-name: openai-demo
          publish-profile: <publishData><publishProfile profileName="openaidemo - Web Deploy" publishMethod="MSDeploy" publishUrl="openaidemo.scm.azurewebsites.net:443" msdeploySite="openaidemo" userName="$openaidemo" userPWD="S4e0bnAPoG5mW5fExzLeu0msJS1zLTeNNiXZdZtiYji4tGT8jM4kMkNoaDM9" destinationAppUrl="https://openaidemo.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="http://windows.azure.com" webSystem="WebSites"><databases /></publishProfile><publishProfile profileName="openaidemo - FTP" publishMethod="FTP" publishUrl="ftps://waws-prod-dm1-291.ftp.azurewebsites.windows.net/site/wwwroot" ftpPassiveMode="True" userName="openaidemo\$openaidemo" userPWD="S4e0bnAPoG5mW5fExzLeu0msJS1zLTeNNiXZdZtiYji4tGT8jM4kMkNoaDM9" destinationAppUrl="https://openaidemo.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="http://windows.azure.com" webSystem="WebSites"><databases /></publishProfile><publishProfile profileName="openaidemo - Zip Deploy" publishMethod="ZipDeploy" publishUrl="openaidemo.scm.azurewebsites.net:443" userName="$openaidemo" userPWD="S4e0bnAPoG5mW5fExzLeu0msJS1zLTeNNiXZdZtiYji4tGT8jM4kMkNoaDM9" destinationAppUrl="https://openaidemo.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="http://windows.azure.com" webSystem="WebSites"><databases /></publishProfile></publishData>
          images: index.docker.io/marwynthedev/openai-demo:${{ github.sha }}
        
